#include<bits/stdc++.h>
using namespace std;

#define optimize() ios_base::sync_with_stdio(0);cout.tie(0);
#define endl '\n'

int main()
{
    /*
    optimize();

    pair<string, int> p;

    p.first = "Nilima";
    p.second = 01;

    cout << p.first << endl;
    cout << p.second << endl;

    pair<string, vector<int>> p1;

    p1.first = "Nilima";
    p1.second = { 3, 7, 2 };

    cout << p1.first << endl;

    for ( auto u : p1.second ) cout << u << " ";
    cout << endl;

    pair<int, int> p2;

    p2 = make_pair ( 3, 4 );

    p2.first++;
    p2.second++;
    cout << p2.first << " " << p2.second << endl;

    pair<int, int> p3;

    p3 = {2, 3};

    cout << p3.first << " " << p3.second << endl;

    pair<string, vector<int>> p4;

    p4 = { "nilima", {1, 2, 3, 4} };

    cout << p4.first << " " << p4.second.size() << endl;


    pair<int , int> p5, p6;

    p5 = { 3, 3 };
    p6 = { 3, 3 };

    if ( p5 < p6 ) cout << "Yes"  << endl;
    else cout << "No" << endl;

    pair<int, int> p7, p8;

    p7 = { 6,3 };
    p8 = { 4,9 };

    pair<int, int> p9 = min ( p7, p8 );

    cout << "Min : " << p9.first << " " << p9.second << endl;

    pair<int, int> p10, p11;

    p10 = { 3, 9 };
    p11 = { 8, 2 };

    pair<int, int> p12 = max ( p10, p11 );

    cout << "Max : " << p12.first << " " << p12.second << endl;


    vector<pair<int, int>> v;

    v.push_back ( { 6, 5 } );
    v.push_back ( { 2, 3 } );
    v.push_back ( { 4, 5 } );
    v.push_back ( { 6, 1 } );
    v.push_back ( { 1, 9 } );

    sort ( v.begin(), v.end() );

    for ( auto u : v ) cout << u.first << " " << u.second << endl;

    cout << endl;

    vector<pair<int, int>> v1;

    v1.push_back ( { 6, 5 } );
    v1.push_back ( { 2, 3 } );
    v1.push_back ( { 4, 5 } );
    v1.push_back ( { 6, 5 } );
    v1.push_back ( { 1, 9 } );

    sort ( v1.rbegin(), v1.rend() );

    for ( auto u : v1 ) cout << u.first << " " << u.second << endl;

    cout << endl;

    pair<int, int> p13[] = { { 6, 5 }, { 2, 3 }, { 4,5 }, { 6, 1 }, { 1, 9 } };

    sort ( p13, p13 + 5 );

    for ( int i = 0; i < 5; i++ ) cout << p13[i].first << " " << p13[i].second << endl;

    vector< pair< string, int> > v2;

    v2.push_back ( { "Nilima", 02 } );
    v2.push_back ( { "Tasnim", 03 } );
    v2.push_back ( { "Bappy", 01 } );
    v2.push_back ( { "shuvon", 34 } );
    v2.push_back ( { "Nilima", 01 } );


    sort ( v2.begin(), v2.end() );

    for ( auto u : v2 ) cout << u.first << " " << u.second << endl;


    cout << endl << endl;

    vector< pair< string, int> > v3;

    v3.push_back ( { "jfkfds", 02 } );
    v3.push_back ( { "Tasdvnim", 03 } );
    v3.push_back ( { "Tasdvnim", 03 } );
    v3.push_back ( { "Tasfdvgnim", 03 } );
    v3.push_back ( { "Bafdfppy", 01 } );
    v3.push_back ( { "shsuvon", 34 } );
    v3.push_back ( { "shuvdfdon", 34 } );
    v3.push_back ( { "Nilidfma", 01 } );
    v3.push_back ( { "Nilidfma", 01 } );
    v3.push_back ( { "Nilidfma", 01 } );


    sort ( v3.begin(), v3.end() );

    int sz = unique ( v3.begin(), v3.end() ) - v3.begin();

    for ( int i = 0; i < sz; i++ ) cout << v3[i].first << " " << v3[i].second << endl;

    */




    return 0;
}
